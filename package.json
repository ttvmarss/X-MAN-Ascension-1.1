{
  "name": "jarvis",
  "version": "2.0.0",
  "description": "JARVIS — voice AI assistant, Node.js + browser, Windows 10 + iPhone",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build-frontend": "cd frontend && npm run build",
    "setup": "npm install && cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "msedge-tts": "^1.3.7",
    "qrcode-terminal": "^0.12.0",
    "selfsigned": "^2.4.1",
    "ws": "^8.18.0"
  }
}
